namespace Orkis.Supervision;

/// <summary>Well-known supervisor registration keys.</summary>
public static class SupervisorKeys
{
    /// <summary>The key used when a run does not specify a supervisor.</summary>
    public const string Default = "default";
}
